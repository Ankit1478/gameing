/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com',"wallpapercave.com","assets.aceternity.com" ],
      },
};

export default nextConfig;
